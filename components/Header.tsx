import CurvedTriangle from "./CurvedTriangle";

export default function Header() {
  return (
    <header className="relative">
      <div>
        <CurvedTriangle />
      </div>

      <nav className="container  mx-auto -mt-5 px-4 lg:px-0">&nbsp;</nav>
    </header>
  );
}
